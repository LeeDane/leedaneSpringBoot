package com.cn.leedane.utils;

/**
 * 分页计算开始和结束页数的工具类
 * @author LeeDane
 * 2016年7月12日 上午10:30:44
 * Version 1.0
 */
public class PagingUtil {
	/*private PageBean pageBean;
	
	private int count = 0;  //总数
	private int pageSize = 10; //每页显示的记录数
	private int pageCount = 0; //总分页的页数
	private int pageCurrent = 1;  //当前的页数
	private boolean isPagging = true;  //是否分页
	
	public Pagingutil(PageBean pageBean) {
		this.pageBean = pageBean;
		if(pageBean != null){
			this.count = pageBean.getCount();
			this.pageSize = pageBean.getPageSize();
			this.pageCount = pageBean.getPageCount();
			this.pageCurrent = pageBean.getPageCurrent();
			this.isPagging = pageBean.isPagging();
		}
	}
	
	public int returnStart(){
				
		return 1;
	}
	
	public int returnEach(){
			
		return 1;
	}*/
	
}
